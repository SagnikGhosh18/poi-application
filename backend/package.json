{
  "name": "poi-sharing-backend",
  "version": "1.0.0",
  "description": "Backend API for POI sharing mobile application",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon src/app.js",
    "test": "jest",
    "db:setup": "cat src/scripts/ddl.sql | docker-compose exec -T postgres psql -U postgres -d poi_sharing_app",
    "db:seed": "cat src/scripts/seed.sql | docker-compose exec -T postgres psql -U postgres -d poi_sharing_app",
    "db:init": "npm run db:setup && npm run db:seed",
    "db:reset": "docker-compose down -v && docker-compose up -d postgres && npm run db:init",
    "build": "npm install",
    "railway:migrate": "node -e \"require('child_process').exec('cat src/scripts/ddl.sql | psql \\$DATABASE_URL', (err,stdout,stderr) => { if(err) console.error(err); else console.log('Migration complete'); })\"",
    "railway:seed": "node -e \"require('child_process').exec('cat src/scripts/seed.sql | psql \\$DATABASE_URL', (err,stdout,stderr) => { if(err) console.error(err); else console.log('Seed complete'); })\"",
    "postdeploy": "npm run railway:migrate && npm run railway:seed"
  },
  "dependencies": {
    "aws-sdk": "^2.1469.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-async-errors": "^3.1.1",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "joi": "^17.11.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "pg": "^8.11.3",
    "sharp": "^0.32.6",
    "uuid": "^9.0.1",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.0.1",
    "supertest": "^6.3.3"
  },
  "keywords": [
    "poi",
    "social",
    "mobile",
    "api"
  ],
  "engines": {
    "node": "22.x"
  },
  "author": "Your Name",
  "license": "MIT"
}
